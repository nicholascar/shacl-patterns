### Jekyll Commands
#### Launch new site
jekyll new . --force

#### Serve locally
bundle exec jekyll serve

to fix dependencies: 
    bundle update && bundle install
    
    then 
    
    bundle add webrick